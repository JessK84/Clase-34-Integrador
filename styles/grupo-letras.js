// Crear un programa que pida al usuario ingresar una letra y mostrar en un mensaje a qué grupo pertenece.
const letra = prompt("Ingrese una lera");


if (letra === "a" || letra === "e" || letra === "i" || letra === "o" || letra === "o") {
    alert("Pertenece al Grupo A");
}
if (letra === "b" || letra === "c" || letra ==="d" || letra ==="f" || letra === "g" ) {
    alert("Pertenece al Grupo B");
}
if (letra === "h" || letra === "j" || letra ==="k" || letra ==="lf" || letra === "l" ) {
    alert("Pertenece al Grupo C");
}
if (letra === "n" || letra === "p" || letra ==="q" || letra ==="r") {
    alert("Pertenece al Grupo D");
}
if (letra === "s" || letra === "t" || letra ==="v" || letra ==="w" || letra === "z" || letra === "z" ) {
    alert("Pertenece al Grupo E");
}

